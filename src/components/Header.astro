---
import Logo from '../components/Logo.astro';

const navItems = [
  { linkText: 'Home', href: '/' },
  { linkText: 'Home Owners', href: '/Home Owners' },
  { linkText: 'Property Managers', href: '/Property Managers' },
  {
    linkText: 'Expertise',
    href: '/Expertise',
    children: [
      { linkText: 'Expertise 1', href: '/Expertise/1' },
      { linkText: 'Expertise 2', href: '/Expertise/2' },
      { linkText: 'Expertise 3', href: '/Expertise/3' },
      { linkText: 'Expertise 4', href: '/Expertise/4' },
      { linkText: 'Expertise 5', href: '/Expertise/5' },
      { linkText: 'Expertise 6', href: '/Expertise/6' },
      { linkText: 'Expertise 7', href: '/Expertise/7' },
      { linkText: 'Expertise 8', href: '/Expertise/8' },
      { linkText: 'Expertise 9', href: '/Expertise/9' },
      { linkText: 'Expertise 10', href: '/Expertise/10' },
    ],
  },
  { linkText: 'Instant Quote', href: '/blobs' },
];
---

<nav class="flex flex-wrap items-center gap-4 pt-6 pb-16 sm:pt-12 sm:pb-24">
  <a href="/" class="shrink-0">
    <Logo />
  </a>

  {
    !!navItems?.length && (
      <ul class="flex flex-wrap items-center gap-x-4 gap-y-1">
        {navItems.map((item) => (
          <li class:list={[item.children?.length ? 'relative group' : '']}>
            <a
              href={item.href}
              class="inline-block px-1.5 py-1 sm:px-3 sm:py-2 hover:underline focus:underline"
            >
              {item.linkText}
            </a>

            {item.children?.length ? (
              <div
                class="
                  invisible opacity-0 pointer-events-none
                  absolute left-0 top-full z-50 mt-2
                  min-w-56
                  rounded-md border border-slate-200 bg-white shadow-lg
                  transition
                  group-hover:visible group-hover:opacity-100 group-hover:pointer-events-auto
                  group-focus-within:visible group-focus-within:opacity-100 group-focus-within:pointer-events-auto
                "
              >
                <ul class="py-2">
                  {item.children.map((child) => (
                    <li>
                      <a
                        href={child.href}
                        class="block px-4 py-2 text-slate-900 hover:bg-slate-100 focus:bg-slate-100"
                      >
                        {child.linkText}
                      </a>
                    </li>
                  ))}
                </ul>
              </div>
            ) : null}
          </li>
        ))}
      </ul>
    )
  }
</nav>
